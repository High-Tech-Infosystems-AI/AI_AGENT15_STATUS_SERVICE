# Environment configuration for AI_AGENT15_STATUS_SERVICE (Status + Notification)

# Logging
STATUS_AGENT_LOG="/home/supriyo/ai_agents_staging/LOGS"

# Authentication
AUTH_SERVICE_URL="http://localhost:8085/ats/verify-token"

# JWT
ACCESS_TOKEN_EXPIRE_HOURS=24
JWT_SECRET_KEY="h7ahasye8172#as819adh1COD797mTdAAA"
JWT_ALGORITHM="HS256"

# Database (shared with other services — same MySQL instance)
DB_HOST="localhost"
DB_PORT="3306"
DB_NAME="ats_staging"
DB_USER="supriyo"
DB_PASSWORD="hti@1234"

# Redis (used for task progress, notification pub/sub, caching)
REDIS_HOST=localhost
REDIS_PORT=6380
REDIS_DB=0
REDIS_PASSWORD=""

# Base URL for this service
BASE_URL="http://localhost:8115"

# Consul service discovery
CONSUL_HOST=localhost
CONSUL_PORT=8500
CONSUL_ENABLED=true
CONSUL_HEALTH_CHECK_ENABLED=false
CONSUL_SERVICE_PORT=8115
CONSUL_SERVICE_EXTERNAL_PORT=
CONSUL_SERVICE_EXTERNAL_IP=127.0.0.1
CONSUL_SERVICE_PATH=/status
CONSUL_SERVICE_AUTH=mixed

# =============================================================================
# NOTIFICATION SERVICE CONFIG
# =============================================================================
# NOTE: The notification service uses the same Redis and MySQL as above.
# No additional config needed — it reuses REDIS_HOST/PORT/DB and DB_HOST/PORT/NAME.
#
# Before first run, execute the SQL migration:
#   mysql -u $DB_USER -p $DB_NAME < migrations/create_notification_tables.sql
#
# The migration creates 4 tables and seeds 66 auto-notification event configs.
# =============================================================================
